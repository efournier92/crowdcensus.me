<div class="sticky">

<nav class="top-bar" data-topbar>
  <div class="top-bar-section" style="border-right:1px #d9d9d9 solid;">
    <ul class="left">
    <li class="has-dropdown">
      <a>CrowdCensus</a>
      <ul class="dropdown">
        <li><%= link_to 'Home', censuses_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to 'Profile', user_path(current_user) %></li>
          <li><%= link_to 'New Census', new_census_path %></li>
        <% end %>
      </ul>
    </li>
  </ul>
    </ul>
    <ul class="right">
      <% if user_signed_in? %>
        <li><%= link_to 'Log Out', destroy_user_session_path,
          :method=>'delete' %></li>
      <% else %>
        <li><%= link_to 'Log In', new_user_session_path %></li>
      <% end %>
    </ul>
  </div>
</nav>

</div>
